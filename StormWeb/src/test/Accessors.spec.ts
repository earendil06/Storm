import {Accessor} from "../resources/Accessor";

test('execute block command with no argument', async () => {
    // const expected = "I have the high ground";
    // const actual = await Accessor.loadFileByName("atestfile");
    // console.log(expected);
    // console.log(actual);
    //TODO find a way to test the accessors without ajax and the browser localStorage
});